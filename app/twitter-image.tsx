// Twitter uses the same card as Open Graph. Kept as a thin re-export so
// there's only one ImageResponse template to maintain.
export { alt, size, contentType } from "./opengraph-image";
export { default } from "./opengraph-image";
